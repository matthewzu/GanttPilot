# 更新日志 / Changelog

## [1.17.4] - 2026-07-01

### 改进 / Improved
- 🔤 **工具栏按钮全部改为英文缩写** — 放弃 Unicode 符号（在 Windows CTk 字体下渲染模糊），统一使用清晰的英文缩写：Un/Re（撤销/恢复）、+/E/V/X（增/编辑/查看/删除）、Cp/Ct/Ps/Dp（复制/剪切/粘贴/克隆）、Up/Dn（上移/下移），所有按钮 tooltip 显示完整含义

## [1.17.3] - 2026-07-01

### 改进 / Improved
- 🎨 **工具栏按钮分组配色** — CRUD 组蓝色(#2563EB)、删除红色(#DC2626)、剪贴板组青绿色(#0D9488)、其余默认灰，参考 CalcPaper 风格
- 🌳 **树节点颜色区分** — 项目蓝色、需求紫色、计划执行青绿、里程碑琥珀、进行中计划绿色、已完结/活动灰色
- 🎨 **色彩主题回归 "blue"** — 与 CalcPaper 保持一致，默认外观模式改回 "System"

## [1.17.2] - 2026-07-01

### 修复 / Fixed
- 🎨 **配色层级优化** — 浅色模式背景从纯白改为 `#FAFAFA`（柔和灰白），深色模式从 `#1E1E1E` 改为 `#2D2D2D`（中灰），增加面板/表头/背景三层灰度层级
- 🧩 **原生组件样式统一** — `apply_treeview_theme()` 扩展覆盖 Notebook 标签页、PanedWindow、TFrame、TLabel 等所有 ttk 组件，深浅模式下视觉一致
- ⚙️ **默认浅色模式** — 默认外观从 "System" 改为 "Light"，避免 Windows 深色系统下 CTk 深色与原生组件白色混搭

## [1.17.1] - 2026-07-01

### 修复 / Fixed
- 🎨 **工具栏按钮配色修复** — 工具栏按钮改为透明背景 + hover 灰色高亮，不再是蓝色大块挤在一起
- 🌙 **深色模式配色优化** — 色彩主题从 "blue" 切换为 "dark-blue"（中性灰色调），Canvas/Treeview 背景改为 VS Code 风格 `#1E1E1E`，分组表头颜色适配深色模式
- 🧱 **布局帧透明化** — 工具栏/搜索栏等布局 Frame 默认透明，消除深色模式下的色块割裂
- 🔲 **分隔线自适应** — 工具栏竖线分隔改为深浅自适应灰色

### 新增 / Added
- 🖱️ **甘特图鼠标滚轮缩放** — Ctrl+滚轮放大/缩小甘特图，普通滚轮垂直滚动

## [1.17.0] - 2026-07-01

### 新增 / Added
- 🌙 **深色模式 (Dark Mode)** — 配置对话框新增外观模式选择器（跟随系统/深色/浅色），实时切换主题，甘特图画布和树视图自动适配深色背景
- 🎨 **CustomTkinter 全量迁移 (CustomTkinter Migration)** — 主窗口、工具栏、对话框全面迁移到 CustomTkinter 现代 UI 框架，圆角按钮、暗色面板，视觉更现代
- 🔄 **优雅回退 (Graceful Fallback)** — 未安装 customtkinter 时自动回退到标准 tkinter，确保兼容性

## [1.16.0] - 2026-06-30

### 新增 / Added
- 🔍 **左侧搜索过滤 (Tree Search Filter)** — 项目树上方新增搜索框，输入关键字实时过滤节点，匹配节点蓝色高亮显示，非匹配节点自动隐藏，父节点自动展开以显示匹配项

### 改进 / Improved
- 🎨 **树节点图标优化 (Tree Node Icons)** — 优化各层级节点的 emoji 图标以提升辨识度：项目(📂)、需求分析(📋)、需求(📝)、任务(🔧)、计划执行(📊)、里程碑(🚩)、进行中计划(▶)、已完结计划(✅)、活动(⏱)

## [1.15.0] - 2026-06-30

### 改进 / Improved
- 🎨 **甘特图圆角任务条 (Rounded Task Bars)** — 任务条从直角矩形改为圆角矩形，DrawBackend 新增 `rounded_rectangle` 抽象方法，CanvasBackend 和 PillowBackend 均已实现
- 🎨 **甘特图行交替底色 (Alternating Row Backgrounds)** — 偶数行白色、奇数行浅灰蓝（#F4F6F8），里程碑行保持原有背景色不变，提升行间可辨识度
- 🖱️ **甘特图悬浮高亮与提示 (Hover Highlight & Tooltip)** — 鼠标悬停在任务条上时条形变亮，300ms 后弹出浮窗显示任务名称、执行者、起止日期、工期和进度（仅 GUI，不影响 PNG 导出）

## [1.14.0] - 2026-06-30

### 改进 / Improved
- 🎨 **工具栏分组优化 (Toolbar Grouping)** — 工具栏按钮按功能分组并用竖线分隔：撤销/恢复 │ 增删改查 │ 剪贴板 │ 排序，视觉层次更清晰，功能辨识度更高

## [1.13.2] - 2026-06-29

### 修复 / Fixed
- 🪟 **所有编辑对话框支持最大化 (All Edit Dialogs Maximizable)** — 添加计划、编辑计划、添加里程碑、编辑里程碑、添加需求、编辑需求、添加任务、编辑任务、创建项目等所有编辑类对话框现在都支持最大化按钮

## [1.13.1] - 2026-06-29

### 修复 / Fixed
- 🪟 **弹窗最大化按钮修复 (Dialog Maximize Fix)** — 移除文本密集型对话框的 `transient(parent)` 调用，解决 Windows 上最大化按钮不显示的问题。受影响对话框：查看详情、配置、MCP 配置、编辑项目、添加活动、编辑活动
- 📋 **推送内容检测增强 (Push Content Detection)** — 未推送内容摘要现在检查所有项目文件（而非仅 project.json），并显示具体变更的文件名

## [1.13.0] - 2026-06-29

### 新增 / Added
- ✅ **推送确认提示 (Push Confirmation)** — 手动推送前弹出确认对话框，显示项目名、目标分支和待推送内容摘要（commit 消息列表），避免误操作
- 📋 **退出推送内容展示 (Exit Push Content Summary)** — 退出时的推送提示现在显示未推送的变更内容，让用户清楚知道将要推送什么
- 🔄 **Undo/Redo 同步 Git (Undo/Redo Git Sync)** — 撤销和恢复操作现在同步创建 Git commit，确保项目树回退时 Git 历史也同步回退，历史标签页可见变更
- 📝 **非 Git 操作日志 (Non-Git Operation Logging)** — 日志文件现在记录配置修改、MCP 服务启动/停止等非 Git 操作，方便问题排查
- 🪟 **弹窗最大化按钮 (Dialog Maximize Button)** — 查看详情、配置、MCP 配置、编辑项目、编辑活动、添加活动等文本密集型对话框现在支持最大化，方便查看和填写大段文字

## [1.12.0] - 2026-06-22

### 新增 / Added
- 👥 **项目成员管理 (Project Members)** — 在项目级别配置成员名字和对应缩写（编辑项目 → 成员字段，格式 `全名:缩写`），添加活动时执行者字段变为下拉选择，避免缩写拼写错误导致统计出错。未配置成员时保持自由输入（向后兼容）。

## [1.11.0] - 2026-06-18

### 改进 / Improved
- 🧹 **工具栏精简 (Toolbar cleanup)** — 不常用按钮（EN/中、A+、A-、⟳检查更新、?帮助、🔌MCP服务）合并到"⚙配置"和"⋯更多"两个入口中，工具栏更简洁
- ⚙️ **配置集成语言和字体 (Language & font in Config)** — 中英文切换和字体大小设置移入配置对话框，保存后立即生效
- ⋯ **更多菜单 (More menu)** — 新增"⋯"弹出菜单，承载检查更新、帮助、MCP服务、关于（含版本号）等不常用功能
- ✂️ **剪切按钮提示 (Cut button tooltip)** — 剪切按钮添加 tooltip 提示，显示"剪切 (Ctrl+X)"
- 📅 **活动日期默认今天 (Activity date defaults to today)** — 创建活动时日期字段自动填充当天日期（YYYYMMDD），无需手动输入
- 🔀 **分支切换全局化 (Global branch switching)** — 切换分支时同步刷新左侧项目树、甘特图、工时统计、需求跟踪和历史记录所有视图，而非仅影响右侧某个标签页

## [1.10.3] - 2026-06-18

### 改进 / Improved
- 🔄 **取消自动重启，改为手动重启 (Remove auto-restart, prompt manual reopen)**
  - 更新下载完成后不再自动重启（彻底解决 PyInstaller `_MEI` 临时目录 DLL 加载失败问题）
  - 点击确定后程序退出，用户手动重新打开即为新版本

## [1.10.1] - 2026-06-18

### 修复 / Fixed
- 🔧 **"升级主线格式"菜单检测增强** — 同时检测旧格式（inline milestones）和 v2 格式中的 flat activity 文件，支持从 v2 升级到 per-activity 格式
- 🔧 **迁移工具支持增量升级** — `_do_migration_on_main` 支持两种迁移路径：旧格式→完整拆分、v2 flat activities→per-activity 目录

## [1.10.0] - 2026-06-18

### 新增 / Added
- 📂 **每条活动独立文件** — 活动记录从单个 `activities/{plan_id}.json` 数组文件拆分为 `activities/{plan_id}/{activity_id}.json` 每条活动独立文件 + `_order.json` 排序文件，进一步减少同计划下多人协作的 Git 冲突
- 🔄 **向后兼容三代格式** — 自动识别 v0（单文件）、v1（activities 数组拆分）、v2（per-activity 文件）三种格式并正确加载

### 改进 / Improved
- 🔀 **迁移工具同步更新** — "升级主线格式"、变基前自动迁移、squash-rebase 均输出 per-activity 格式

## [1.9.4] - 2026-06-18

### 修复 / Fixed
- 🔀 **修复 squash-rebase 数据丢失** — 旧逻辑用私有分支数据覆盖主线，丢失他人 PR 内容。改为三方数据合并：以主线为基础，仅应用相对于共同祖先的实际变更

## [1.9.0] - 2026-06-18

### 新增 / Added
- 📂 **v2 完全拆分存储格式** — 项目数据从单一 project.json 拆分为独立文件（需求、里程碑、活动各自独立），大幅减少多人协作时的 Git 合并冲突
  - `project.json` 仅保留项目配置（名称、远端、标签、顺序索引）
  - `requirements/{id}.json` — 每个需求（含任务）独立文件
  - `milestones/{id}.json` — 每个里程碑（含计划元数据）独立文件
  - `activities/{plan_id}.json` — 每个计划的活动记录独立文件
- 🔄 **一键升级主线格式** — 维护者右键菜单 → "升级主线格式"，自动迁移并推送到主分支，其他成员变基时自动跟随迁移
- 🔀 **变基前自动格式迁移** — 检测到主线已升级为新格式时，变基前自动迁移本地数据，避免格式差异导致的冲突
- 💡 **本地项目迁移提示** — 启动时检测本地项目（无远端仓库）是否使用旧格式，提示用户一键迁移

### 改进 / Improved
- 📖 **分支数据读取重构** — 从其他分支读取项目数据时自动识别 v2 格式并正确组装完整数据
- 🔄 **向后兼容** — 自动识别并加载 v0（单文件）、v1（activities 拆分）、v2（完全拆分）三种格式

## [1.8.4] - 2026-06-15

### 修复 / Fixed
- 🔄 **更新重启 DLL 加载失败彻底修复** - 清除传递给重启脚本的 PATH 环境变量中包含 `_MEI` 临时目录的路径，并使用 `start /I` 以初始环境启动新进程，彻底避免旧临时目录残留导致 "Failed to load Python DLL" 报错

## [1.8.3] - 2026-06-15

### 修复 / Fixed
- 🔄 **同步时 commit 失败导致推送卡住** - 修复 `sync()` 中 `git commit` 因无实际变更失败（rc=1）时抛出异常中断整个同步流程的问题，改为 `check=False` 容忍无变更提交，确保后续 push 正常执行

## [1.8.2] - 2026-06-15

### 修复 / Fixed
- 🔢 **版本号修正** - 修复 1.8.1 版本号未更新导致客户端检测不到更新的问题

## [1.8.1] - 2026-06-15

### 修复 / Fixed
- 🔄 **更新重启修复** - 修复重启脚本因环境变量继承导致新进程报 `Failed to load Python DLL _MEI*` 的问题，改用独立 .cmd 脚本 + STARTUPINFO 隐藏窗口 + 延时等待旧进程退出，彻底避免环境变量污染
- 🪟 **对话框切换卡死修复** - 所有弹出对话框改用延迟 grab（deferred grab_set），解决 Alt-Tab 切换窗口后返回时界面卡死的问题
- 🌐 **切换语言不彻底修复** - 切换语言时刷新工具栏按钮提示文字和项目树（需求分析/计划执行分类名称），tooltip 改为动态文本引用避免重复绑定

### 改进 / Improved
- 📁 **日志路径和命名优化** - 日志文件改为 `~/.ganttpilot/ganttpilot.log`（原 git_sync.log），作为通用操作日志记录项目 git 仓库中未记录的重要操作
- 📝 **帮助信息提示日志路径** - 帮助对话框开头显示日志文件路径，方便快速定位
- 🏷️ **配置项重命名** - "Git日志保留天数"改为"日志保留天数"，更准确反映其用途

## [1.8.0] - 2026-06-12

### 新增 / Added
- 📝 **Git 操作日志** - 所有 git 命令（特别是 push/fetch）会记录到本地配置路径下的 `ganttpilot.log` 文件，包含时间戳、命令内容、成功/失败状态和错误信息，方便排查推送失败等问题
- ⚙️ **日志保留天数配置** - 全局配置新增"日志保留天数"（默认 30 天），超过天数的日志文件自动清理

### 改进 / Improved
- 🔍 **推送失败可追溯** - push 失败时完整记录 stderr 输出和重试过程，对比 git bash 排查问题更方便

## [1.7.1] - 2026-05-30

### 修复 / Fixed
- 🛠️ 修复更新后重启报错 `Failed to load Python DLL _MEI*\python39.dll` 的问题
  - 根本原因：VBS 重启脚本的 `ws.Run` 继承了旧进程的 `_MEIPASS2` 环境变量，导致新 exe 去已删除的 `_MEI` 临时目录查找 DLL
  - 修复：改用 PowerShell 重启（与 CalcPaper 方案一致），在启动新进程前显式清除 `_MEIPASS2` 和 `_MEIPASS` 环境变量
  - 启动时自动清理上次更新遗留的 `.old` 文件

## [1.7.0] - 2026-05-30

### 新增 / Added
- 📊 **甘特图分段导出** - 报告中的甘特图不再压缩，改为按配置的最大天数拆分成多段独立图片，每段清晰可读
- ⚙️ **导出图片缩放倍数** - 新增配置项"导出图片缩放倍数"（默认 2.0），控制导出 PNG 的字体和元素大小

### 改进 / Improved
- 📐 **图片宽度自适应** - 每段甘特图自动适配 ~1600px 宽度，不再出现超宽图片在文档中缩放后看不清的问题
- 🏷️ **日期标签防重叠** - 当每天列宽较窄时，header 中的星期和日期自动间隔显示，避免文字重叠
- ⚙️ **配置简化** - 移除"报告图片压缩阈值"和"报告图片最大宽度"两个旧参数，替换为更直观的"甘特图每段最大天数"（默认 90）和"导出图片缩放倍数"（默认 2.0）

### 移除 / Removed
- 🗑️ **compress_threshold** - 不再支持压缩模式，改用分段模式
- 🗑️ **max_chart_width** - 图片宽度现在自动适配，无需手动设置

## [1.6.3] - 2026-05-28

### 修复 / Fixed
- 🔄 **更新重启彻底修复** - 使用 VBS 脚本实现无窗口延迟重启，解决 PyInstaller `_MEI` 临时目录锁定和 DLL 加载失败问题，不再闪烁 cmd 窗口

## [1.6.2] - 2026-05-28

### 修复 / Fixed
- ⌨️ **查看快捷键** - 默认快捷键改为 F4，更直观易用
- 🔌 **MCP 启动报错** - 修复打包后启动 MCP 服务报 "Failed to start embedded python interpreter" 的问题，打包环境改用线程模式运行 MCP server

## [1.6.1] - 2026-05-28

### 修复 / Fixed
- ⌨️ **查看快捷键冲突** - 默认快捷键从 F3 改为 Ctrl+Shift+F，避免与 IDE 快捷键冲突；配置界面中可自定义
- 🪟 **MCP 对话框焦点** - 修复打开 MCP 配置对话框后 Alt-Tab 切换窗口再切回来界面卡死的问题
- 🔄 **更新重启临时目录** - 修复自动更新重启时 PyInstaller 临时目录 `_MEI` 被锁定导致报错的问题，改用延迟启动
- 🔌 **MCP 模板路径** - 修复打包后 MCP 配置模板中脚本路径指向临时目录的问题，改用 `--mcp` 启动参数

## [1.6.0] - 2026-05-28

### 新增 / Added
- 🔌 **MCP Server 支持** - 新增 `ganttpilot_mcp.py`，通过 Model Context Protocol 暴露 25 个工具，允许 AI 助手（Kiro、Claude 等）直接管理项目数据
- 🔌 **MCP 配置界面** - 工具栏新增 🔌 按钮，打开 MCP 配置对话框，支持启动/停止 MCP 服务、查看状态、一键复制 Kiro/Claude/通用配置模板
- 👁 **查看详情** - 树形图每个节点新增"查看详情"功能（右键菜单 + 工具栏 👁 按钮 + F3 快捷键），只读展示节点完整信息，支持滚动和 Ctrl+滚轮缩放
- 💬 **需求跟踪悬浮提示** - 需求跟踪标签页鼠标悬停时显示可滚动、可缩放的详情浮窗

### 改进 / Improved
- ⚙️ **MCP 自动启停** - 配置中保存 MCP 启用状态，下次启动自动恢复；关闭应用时自动停止 MCP 服务进程

## [1.5.1] - 2026-05-21

### 新增 / Added
- 📊 **需求跟踪工时差异** - 需求跟踪标签页新增"差异"列，已完结计划显示超出(+)或少用(-)工时
- 📊 **项目级工时统计** - 工时统计"按项目查看"模式新增项目级超出/少用工时汇总行（仅统计已完结计划）

### 修复 / Fixed
- 📊 **里程碑工时统计修正** - 按里程碑统计的超出/少用工时现在仅计算已完结计划的实际工时与计划工时差异，与报告保持一致
- 📄 **报告里程碑汇总修正** - 报告中"里程碑工时汇总"章节的超出/少用统计现在仅计算已完结计划，不再包含进行中的计划

## [1.5.0] - 2026-05-20

### 新增 / Added
- ✂️ **剪切功能** - 任务、计划、活动、需求、里程碑支持剪切操作（Ctrl+X），剪切后粘贴不添加"(Copy)"后缀
- 📊 **超出/少用工时统计** - 按里程碑和按计划查看工时时显示超出和少用的工时差异（仅对已完结计划生效）

### 改进 / Improved
- 📌 **粘贴位置优化** - 剪切或粘贴时插入到当前选中节点的后面（紧挨选中节点），而非追加到末尾
- 📄 **报告超出/少用统计** - 报告的需求跟踪(§5)、项目总工时(§6)、按里程碑统计(§9)、按计划统计(§10)章节均显示已完结计划的超出/少用工时
- 📝 **导出报告文件名** - 导出报告时默认文件名添加 `_summary` 或 `_detail` 后缀，便于区分

## [1.4.1] - 2026-05-15

### 修复 / Fixed
- 🔕 **退出推送提示优化** - 仅在有未推送的本地提交或未暂存的 project.json 变更时才弹出推送提示，已同步的项目不再重复提示

## [1.4.0] - 2026-05-15

### 新增 / Added
- 📊 **报告类型选择** - 生成报告时弹出对话框，可选"概况"（不含工时）或"细节"（完整报告）
- 🚀 **退出时推送提示** - 关闭应用时自动提示是否推送私有分支到远端，确认即推送
- 🔄 **启动时自动拉取主线** - 打开应用时自动拉取所有项目的远端主线更新
- 👤 **活动执行者默认值** - 添加活动时执行者字段自动填充提交者名称（优先级: 活动 > 项目 > 全局），并显示提示信息

### 改进 / Improved
- 🔒 **Git 提交仅包含 project.json** - commit 和 sync 操作只暂存 project.json 文件，排除其他文件（如报告、临时文件等）

## [1.3.5] - 2026-05-08

### 修复 / Fixed
- 🔒 **个人信息不再写入项目文件** - `committer_name`、`committer_email`、`priv_branch` 三个字段完全从 project.json 中移除，改为仅存储在用户本地全局配置中。即使其他人的合并请求意外带入这些字段，也不会影响本地分支设置，彻底解决"拉取后本地分支变成别人分支"的问题

## [1.3.4] - 2026-05-07

### 修复 / Fixed
- 🧊 **Git 操作不再卡死界面** - 同步(Push)、拉取(Pull)、变基(Rebase) 等 git 操作全部改为后台线程执行，不再阻塞 GUI 主线程
- 📄 **迁移逻辑不再污染工作区** - 加载项目时移除旧 committer 字段的迁移逻辑不再写入磁盘，避免产生 unstaged changes 导致 rebase 失败

## [1.3.3] - 2026-05-07

### 修复 / Fixed
- 🔗 **更新提示链接修正** - CHANGELOG 链接改为 `blob/master` 路径，README 链接改为 `#readme` 风格，与帮助信息一致
- 🪟 **更新对话框焦点修复** - 更新提示窗口注册为活动对话框，切换应用后返回时焦点正确恢复，不再阻塞主界面；同时防止重复打开

## [1.3.2] - 2026-05-07

### 新增 / Added
- 🔄 **同步主线菜单** - 项目右键菜单新增"同步主线"按钮，随时手动将私有分支变基到最新 main（fetch + rebase + force-push）

### 改进 / Improved
- 💬 **工具栏按钮提示** - 更新(⟳)、帮助(?)、设置(⚙) 按钮鼠标悬停时显示提示信息
- 📐 **对话框尺寸优化** - 编辑项目对话框(480×450)和 Git 配置对话框(560×380)初始尺寸增大，内容显示更完整
- 🔀 **Rebase 后自动推送** - 变基成功后自动使用 `--force-with-lease` 推送私有分支到远端，无需手动推送
- 📋 **帮助对话框改进** - 帮助信息改为带滚动条的对话框，顶部添加可点击的 README 链接
- 🔒 **提交者信息 Fallback** - 创建项目时提交者获取顺序统一为：手动输入 → 全局配置 → 系统 git config，获取不到则阻止创建
- 📖 **README 全面更新** - 工具栏完整列表、配置表格修正、本地/协作模式字段说明、变基流程、同步主线操作

## [1.3.1] - 2026-05-07

### 改进 / Improved
- 👤 **项目级提交者配置** - 每个项目可以设置独立的提交者名称和邮箱，留空则自动使用全局配置
- 📝 **提交者字段移至项目编辑** - 提交者信息从 Git 配置对话框移到项目编辑对话框，本地模式和协作模式创建项目时均可填写
- 💡 **全局 Fallback 提示** - 项目编辑对话框中提交者字段留空时显示"将使用全局配置: xxx <xxx@xxx>"提示
- 🔒 **提交者信息不入库** - 提交者信息保存在本地全局配置的 `project_committers` 字典中（按项目名索引），不会被提交到 git 仓库

## [1.3.0] - 2026-05-07

### 新增 / Added
- 🔗 **更新对话框可点击链接** - 软件更新前弹出对话框包含可点击的 CHANGELOG 链接，更新完成后包含可点击的 README 链接
- 🔄 **更新后自动重启** - 下载更新完成后自动重启应用，无需手动操作
- ⚙️ **提交者信息全局化** - 提交者名称和邮箱从项目级别移至全局配置，所有项目共享，避免 PR 合并后信息不一致导致拉取失败
- 🧹 **自动迁移** - 加载项目时自动清除 project.json 中的旧提交者字段，下次同步即生效
- 🔀 **分支名安全生成** - 自动生成私有分支名时将空格等非法字符替换为下划线（如 "Kevin Fang" → `priv_Kevin_Fang`）
- 🔀 **旧分支自动重命名** - 如果本地存在旧的无效分支名（含空格），init_repo 时自动重命名为合法名称

### 修复 / Fixed
- 🐛 **PR 合并后拉取失败** - 修复提交者名称含空格时生成无效分支名（如 `priv_Kevin Fang`）导致 git pathspec 错误的问题
- 🐛 **Git 配置对话框** - 移除项目级提交者字段，简化为仅配置远端和私有分支名

### 向后兼容说明
- 已有项目中的 `committer_name`/`committer_email` 字段会在加载时自动清除并写回文件
- 全局配置（`~/.ganttpilot/config.json`）新增 `committer_name` 和 `committer_email` 字段
- 首次使用协作模式创建项目时输入的提交者信息会自动保存到全局配置

## [1.2.0] - 2026-05-07

### 新增 / Added
- 🔄 **手动更新检测** - 工具栏新增 ⟳ 按钮，手动检查是否有新版本可用，检测期间按钮自动禁用防止重复点击
- 📝 **活动描述字段** - 活动新增多行描述输入框，与需求描述具有相同的编辑特性（带滚动条），向后兼容旧数据
- 🔀 **fetch 后自动更新 main** - 拉取/同步后自动将本地 main 分支快进到 origin/main，无需手动切换分支执行 pull
- 💬 **rebase 提示** - main 分支有更新时自动弹窗询问是否将私有分支变基到最新 main，冲突时自动中止并提示
- 🚫 **禁止向 main 推送** - sync 操作仅推送私有分支，不会向远端 main 分支推送
- ✅ **私有分支名校验** - 创建/编辑项目时校验私有分支名称，禁止输入 "priv"、与主分支同名或含非法字符

### 修复 / Fixed
- 📐 **对话框居中** - 所有模态对话框（添加项目、编辑计划等 15 个）改为居中于主窗口显示，修复从 (0,0) 开始导致被任务栏遮挡的问题
- 🔧 **clone_repo 分支名修复** - clone_repo 使用用户配置的 priv_branch 名称创建私有分支，而非硬编码 "priv"

### 向后兼容说明
- 已有项目中使用 "priv" 分支的配置继续正常工作，不强制迁移
- 旧活动数据加载时自动填充空描述字段

## [1.1.0] - 2026-05-06

### 新增 / Added
- 🆕 **项目创建双模式** - "添加项目"对话框拆分为本地模式和协作模式，通过单选按钮切换
- 🏠 **本地模式** - 仅需填写项目名称和描述（可选），快速创建本地项目
- 🤝 **协作模式** - 一步完成 Git 配置（远端地址、分支、用户名、密码、提交者信息、私有分支），无需单独打开 Git 配置对话框
- 💡 **输入提示** - 所有输入框添加占位提示信息（PlaceholderEntry），引导用户正确填写
- ✅ **字段验证** - 协作模式下验证远端仓库地址格式，提交者信息为空时自动检测系统 Git 配置
- 📐 **对话框自适应** - 切换模式时对话框自动调整尺寸

## [1.0.2] - 2026-04-24

### 修复 / Fixed
- 🪟 **窗口切换卡死** - 修复对话框打开时切换到其他窗口再切回来界面卡死的问题（Windows grab 焦点恢复）
- 🚫 **对话框重复打开** - 修复可以重复点击按钮打开多个对话框实例的问题，所有对话框现在都有防重复守卫

## [1.0.1] - 2026-04-24

### 新增 / Added
- ↕️ **活动节点排序** - 活动节点支持上移/下移操作（工具栏按钮和右键菜单）

### 修复 / Fixed
- 📦 **打包缺失示例** - PyInstaller 打包时包含 examples/ 目录，修复打包程序加载示例报错
- 🔑 **示例项目提交者身份** - 示例项目使用演示身份（GanttPilot），不再泄露本机 Git 配置
- 📝 **初始提交** - 加载示例项目后立即生成初始提交，无需额外操作即可看到历史记录
- ⌨️ **Alt+Up/Down 快捷键** - 修复树控件中 Alt+Up/Down 快捷键被 Treeview 默认行为拦截的问题
- 🔄 **历史记录实时刷新** - 每次提交后自动刷新历史记录和分支选择器

## [1.0.0] - 2026-04-24

### 🎉 v1.0 正式发布 / v1.0 Official Release

GanttPilot 正式发布 1.0 版本，标志着功能体系趋于完整和稳定。

GanttPilot reaches v1.0, marking a complete and stable feature set.

### 改进 / Improved
- 📖 **README 重构** - 从功能列表重组为面向用户的使用指南，按工作流程组织，中英文双语
- 💡 **GUI 帮助重写** - 帮助对话框从简单菜单罗列改为结构化快速参考，涵盖快速上手、界面指南、工时记录、Git 协作等
- 🏷️ **Release 页面优化** - GitHub Release 说明从功能堆砌改为简洁的分类概览

## [0.13.0] - 2026-04-23

### 新增 / Added
- 🚀 **推送/拉取分离** - "同步"菜单拆分为"推送"和"拉取"两个独立操作，推送仅提交并推送到远端，拉取仅从远端获取最新数据
- 🔄 **后台自动检测更新** - 启动后每隔一定时间（默认 5 分钟，可在配置中调整）自动后台 fetch 远端并检测主线更新，有更新时显示横幅提示
- 🏷️ **项目标签管理** - 项目编辑对话框新增标签字段，逗号分隔定义项目级标签列表
- 🏷️ **活动标签选择** - 活动添加/编辑对话框中，标签字段改为下拉选择框，只能从项目定义的标签中选择（项目无标签时保持自由输入）
- ⚙️ **拉取间隔配置** - 配置对话框新增"拉取间隔(分钟)"参数，控制后台自动检测远端更新的频率
- 💡 **跳过日期格式提示** - 计划创建/编辑对话框中跳过日期列表字段增加格式提示

### 改进 / Improved
- 🇨🇳 **中文逗号兼容** - 标签列表、工时时间段、跳过日期列表均支持中文逗号（，）作为分隔符

## [0.12.0] - 2026-04-23

### 新增 / Added
- 📊 **实际工时跟踪** - 需求跟踪标签页和报告中新增"实际工时"列，显示每个关联计划的活动工时总和
- 📊 **项目工时汇总** - 报告"计划进度详情"章节底部新增总计划工时和总实际工时汇总

### 改进 / Improved
- 🕐 **计划工时显示优化** - 需求跟踪标签页中工作量（人日）转换为小时数显示，方便与实际工时对照
- 🕐 **计划工时回退计算** - 报告中计划工时优先使用 planned_hours 字段，为 0 时自动从关联任务的 effort_days × 8 计算
- 🔐 **同步认证回退** - Git 同步时嵌入式认证失败后自动回退到系统 credential helper（如 Windows Credential Manager），解决密码过期导致同步失败的问题

### 修复 / Fixed
- 📝 **报告表格格式修复** - 需求描述中的换行符和管道符在 Markdown 表格中正确转义，避免表格结构被破坏

## [0.11.0] - 2026-04-23

### 新增 / Added
- ⏪ **回退到此处** - 历史记录标签页右键提交记录，选择"回退到此处"执行 `git reset --hard`，丢弃之后所有提交
- ↩️ **撤销此提交** - 历史记录标签页右键提交记录，选择"撤销此提交"执行 `git revert`，生成反向提交保留历史

### 改进 / Improved
- 🔄 **同步策略优化** - 启动时仅自动 fetch（拉取远端更新），不再自动 push；切换项目和关闭窗口时不再自动同步；推送操作仅通过手动同步（右键菜单/Ctrl+S）触发

### 修复 / Fixed
- 🐍 **Python 兼容性** - 修复 `str | None` 类型注解在 Python 3.9 以下版本导致启动崩溃的问题，添加 `from __future__ import annotations`

## [0.10.0] - 2026-04-22

### 新增 / Added
- ⌨️ **键盘快捷键系统** - 新增 ShortcutManager 集中管理快捷键：Ctrl+N 添加、F2 编辑、Delete 删除、Alt+Up/Down 上移/下移、Ctrl+D 克隆、Ctrl+S 同步、F5 刷新
- ⚙️ **快捷键配置** - 配置对话框新增快捷键配置区域，支持自定义快捷键绑定、冲突检测、恢复默认
- 💡 **快捷键提示** - 工具栏按钮 tooltip 和右键菜单显示对应快捷键信息
- ⎋ **ESC 关闭对话框** - 所有 15 个对话框支持 ESC 键快速关闭

### 改进 / Improved
- 🔤 **字体缩放增强** - 通过 TkDefaultFont/TkTextFont 全局字体缩放，所有对话框控件（Entry、Label、Text）跟随缩放
- 📐 **对话框布局优化** - 所有对话框添加 columnconfigure 和 sticky=EW，拉伸时输入框自动扩展
- 🗑️ **配置精简** - 全局配置对话框移除 Git 相关字段（远端地址、用户名、密码），Git 配置仅在项目级别管理

### 修复 / Fixed
- 🪟 **对话框焦点恢复** - 修复打开对话框后 Alt-Tab 切换窗口再切回时无法操作的问题，添加 FocusIn 焦点恢复机制和 _active_dialog 跟踪
- ✏️ **描述框撤销/恢复** - 修复 tk.Text 描述框中 Ctrl+Z/Y 触发全局 UndoManager 而非文本撤销的问题，为所有描述框启用 undo=True 并添加焦点检测
- ↕️ **移动快捷键修复** - 修复任务/需求节点移动快捷键有时不生效的问题，移动后立即更新工具栏按钮状态
- 📅 **活动日期可选** - 活动对话框中日期字段改为可选项，标签显示"可选"提示

## [0.9.1] - 2026-04-21

### 新增 / Added
- ⏱ **工时（小时数）** - 活动新增"工时（小时数）"字段，允许直接输入小时数，与"工时（时间段）"二选一填写，互斥校验并提供明确提示
- 📊 **项目总工时** - 项目报告新增"项目总工时"章节，显示项目合计工时及每位执行者的工时与占比
- 📊 **分组工时合计** - 工时统计界面所有查看模式（按项目/里程碑/计划/标签）均显示每个分组的总工时行
- 📊 **工时占比** - 工时统计界面和项目报告中显示每位执行者工时占当前分组总工时的百分比

### 改进 / Improved
- 🏷️ **工时时段更名** - "工时时段"统一更名为"工时（时间段）"，"工时(小时数)"统一为"工时（小时数）"
- 🌳 **活动树形显示** - 活动在树形图中直接显示小时数（如 `3.0h`），不再显示时间段字符串
- 📝 **示例项目更新** - 示例项目描述和活动内容更新，体现"直接填写小时数"的新用法

## [0.9.0] - 2026-04-21

### 新增 / Added
- ⧉ **节点克隆** - 一键克隆任意节点（项目、需求、任务、里程碑、计划、活动）及其所有子节点，自动重新生成 ID 并添加 "(Copy)" 后缀
- 📋 **复制粘贴** - 复制节点到内存剪贴板，支持跨项目/跨里程碑/跨需求粘贴，自动校验目标位置类型兼容性
- 🛠️ **工具栏按钮** - 新增复制(📋)、粘贴(📌)、克隆(⧉) 三个工具栏按钮，根据选中节点类型和剪贴板内容自动启用/禁用
- ⌨️ **快捷键** - Ctrl+C 复制、Ctrl+V 粘贴
- 📝 **右键菜单** - 所有节点类型的右键菜单增加复制/粘贴/克隆选项，粘贴选项仅在类型兼容时显示

### 改进 / Improved
- 🛠️ **工具栏布局优化** - 按钮按逻辑分组：增/编辑/删除 → 复制/粘贴/克隆 → 上移/下移
- 📋 **右键菜单排序优化** - 统一排序：添加子节点 → 编辑/属性 → 剪贴板操作 → 状态操作 → 工具操作 → 删除

### 修复 / Fixed
- 🧪 修复 hypothesis 属性测试因默认 200ms deadline 导致的间歇性失败，为所有涉及文件 I/O 的测试设置 `deadline=None`

## [0.8.1] - 2026-04-20

### 新增 / Added
- 📋 **需求管理** - 在需求分析节点下创建需求，支持类别、主题和描述
- ✅ **任务管理** - 将需求拆解为任务，支持工作量估算（人日）
- 🔗 **需求跟踪** - 计划关联任务，需求跟踪标签页查看完整跟踪链条
- 🛠️ **统一工具栏** - 添加、编辑、删除、上移、下移按钮
- 📝 **报告增强** - Markdown 报告包含需求分析和需求跟踪章节

## [0.8.0] - 2026-04-19

### 新增 / Added
- 📊 **报告查看模式** - 工时报告支持按项目、按里程碑、按计划、按标签四种查看模式
- 🪟 **窗口状态记忆** - 最大化状态正确保存和恢复
- 📝 **里程碑多行描述** - 里程碑创建/编辑对话框中描述字段改为多行编辑框
